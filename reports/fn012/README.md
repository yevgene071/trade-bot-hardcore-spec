# FN-012 report bundle

This bundle is the canonical FN-012 deliverable for the bounce/breakout source digest.

- `strategy_source_digest_bounce_breakout.md` — formal rule matrix for `BounceFromDensity` / level bounce and `BreakoutEatThrough`.
- `docx-text/*.txt` — extracted text from the four source DOCX files with stable line numbers used by the matrix citations.

The digest covers every rule with source reference, formal condition, required data, expected signal/strategy behavior, and status (`implemented`, `missing`, `ambiguous`, or `live-only`).
